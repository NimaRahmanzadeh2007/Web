const timeText = document.getElementById("time");

let milliseconds = 0;
let  = 0;
let minutes = 0;

function stopwatch() {

    seconds++;


    timeText.textContent = `${minutes}:${seconds}:${milliseconds}`;

}


